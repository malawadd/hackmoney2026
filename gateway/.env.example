# x402 Gateway Environment Variables

# Circle API (Required for real wallet integration)
# Get your API key from https://console.circle.com/
CIRCLE_API_KEY=
CIRCLE_ENTITY_SECRET=
CIRCLE_WALLET_SET_ID=

# Demo Wallet (Optional - pre-created wallet for demos)
DEMO_WALLET_ID=
DEMO_WALLET_ADDRESS=

# EOA Wallet for x402 signTypedData (Optional - for BASE-SEPOLIA)
EOA_WALLET_ID=
EOA_WALLET_ADDRESS=

# Arc Blockchain (Testnet by default)
ARC_RPC_URL=https://rpc.testnet.arc.network
ARC_CHAIN_ID=5042002

# Executor Wallet (Required for on-chain tx submission)
# Create a new wallet and fund it with testnet USDC
EXECUTOR_PRIVATE_KEY=
EXECUTOR_ADDRESS=

# Gemini API (Optional - for AI demo)
GEMINI_API_KEY=

# Server
PORT=3001
NODE_ENV=development

# Security Configuration
# Comma-separated list of allowed origins for CORS (required in production)
ALLOWED_ORIGINS=https://your-frontend.vercel.app,https://arcent.vercel.app
# Maximum requests per minute per IP (default: 30)
RATE_LIMIT_MAX=30

# =====================
# SPENDING LIMITS
# =====================
# All values in USD. These protect agents from runaway spending.

# Maximum amount per single transaction (default: $0.50)
MAX_TRANSACTION_AMOUNT=0.50

# Daily spending limit (default: $10.00)
DAILY_SPENDING_LIMIT=10.00

# Weekly spending limit (default: $50.00)
WEEKLY_SPENDING_LIMIT=50.00

# Monthly spending limit (default: $100.00)
MONTHLY_SPENDING_LIMIT=100.00

# Alert when wallet balance falls below this (default: $5.00)
LOW_BALANCE_THRESHOLD=5.00

# Turso Database (for persistent storage on Vercel)
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=
